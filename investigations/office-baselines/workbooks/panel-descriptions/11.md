### 👤 vs 🌐 11. One person against the organisation over time

| Workbook context | Value |
|---|---|
| Selected period | `{W_TimeRange:start}` to `{W_TimeRange:end}` |
| Person shown | `{W_TargetUser}` *(set this to choose the person)* |

This chart draws one person's activity and the organisation's average on the same chart, over time and using hourly buckets so a one-day report still shows detail. The gap between the two lines shows, bucket by bucket, whether the person is running above or below the typical level. Set the user filter to choose the person.

This is supporting context, not a league table.

#### What the chart shows

| Element | What it means |
|---|---|
| Horizontal axis (`TimeBin`) | The time bucket (hourly). |
| Vertical axis (`Value`) | The number of activity events in that bucket. |
| `Series` = `User` | The selected person's activity in each bucket. |
| `Series` = `Org avg / active user` | The organisation's average activity per active person. |

#### What is included

Everyday staff activity only. Guests, system accounts and listed service accounts are excluded. If the user filter is blank, only the organisation line appears.
